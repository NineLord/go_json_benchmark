package main

import (
	. "fmt"
)

// Shaked-TODO: can't have two mains in the same directory, change those files into main.go and move them into directories, update makefile

func main() {
	Println("Ok!")
	Println("Bye!")
}
